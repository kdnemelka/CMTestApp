<Window x:Class="So.JustNoteIt.Client.Desktop.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:viewModels="clr-namespace:CMTestApp.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{x:Type viewModels:MainViewModel}"
        Title="{Binding WindowTitle}"
        Background="Red"
        Height="300" Width="300">
    <Grid>
        <TextBlock FontSize="48"> Hello there</TextBlock>
        <ContentControl x:Name="ActiveItem"></ContentControl>
    </Grid>
</Window>